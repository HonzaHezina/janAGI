services:
  postgres:
    image: pgvector/pgvector:0.8.0-pg16
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TZ: ${TZ}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10

  n8n:
    image: n8nio/n8n:latest
    restart: unless-stopped
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_TYPE: postgresdb
      DB_POSTGRESDB_HOST: postgres
      DB_POSTGRESDB_PORT: 5432
      DB_POSTGRESDB_DATABASE: ${POSTGRES_DB}
      DB_POSTGRESDB_USER: ${POSTGRES_USER}
      DB_POSTGRESDB_PASSWORD: ${POSTGRES_PASSWORD}

      N8N_HOST: ${N8N_HOST}
      N8N_PORT: ${N8N_PORT}
      N8N_PROTOCOL: ${N8N_PROTOCOL}
      WEBHOOK_URL: ${WEBHOOK_URL}
      N8N_EDITOR_BASE_URL: ${N8N_EDITOR_BASE_URL}

      N8N_ENCRYPTION_KEY: ${N8N_ENCRYPTION_KEY}

      TZ: ${TZ}
      GENERIC_TIMEZONE: ${GENERIC_TIMEZONE}

      N8N_PAYLOAD_SIZE_MAX: 64

      # Optional env (used by some workflows)
      OPENCLAW_BASE_URL: ${OPENCLAW_BASE_URL}
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      N8N_BASE_URL: ${N8N_BASE_URL}
      N8N_API_KEY: ${N8N_API_KEY}
      N8N_INTERNAL_URL: ${N8N_INTERNAL_URL}
      N8N_OPERATOR_EMAIL: ${N8N_OPERATOR_EMAIL}
      N8N_OPERATOR_PASSWORD: ${N8N_OPERATOR_PASSWORD}

    ports:
      - "${N8N_PORT:-5678}:5678"
    volumes:
      - n8n_data:/home/node/.n8n

  mindsdb:
    image: mindsdb/mindsdb:latest
    restart: unless-stopped
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      TZ: ${TZ}
    volumes:
      - mindsdb_data:${MINDSDB_STORAGE:-/root/mdb_storage}
    ports:
      - "47334:47334"
      - "47335:47335"

  clawd_worker:
    build:
      context: ../../services/clawd_worker
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: ${TZ}
      MISTRAL_API_KEY: ${MISTRAL_API_KEY}
    ports:
      - "8090:8090"

volumes:
  postgres_data:
  n8n_data:
  mindsdb_data:
