{
  "name": "WF_10_Turbo_OpenClaw_Run (skeleton)",
  "nodes": [
    {
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "parameters": {
        "path": "turbo/openclaw/run",
        "httpMethod": "POST"
      }
    },
    {
      "name": "OpenClaw /v1/responses",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "url": "={{$env.OPENCLAW_BASE_URL}}/v1/responses",
        "method": "POST",
        "jsonParameters": true,
        "sendBody": true,
        "headerParametersJson": "={{JSON.stringify({ Authorization: 'Bearer ' + $env.OPENCLAW_GATEWAY_TOKEN })}}",
        "bodyParametersJson": "={{JSON.stringify({ model: 'openclaw:main', user: $json.user_key || ('n8n:' + ($json.trace_id || $now)), input: $json.input || $json.prompt || JSON.stringify($json) })}}"
      }
    }
  ],
  "connections": {}
}
